# Testnet Deployment

To be filled in after deployment. Should record, for each step of the
reproducible demo:

```text
1. RWA asset issued          -> <transaction-hash>
2. KYC attestation written   -> <transaction-hash>
3. Deposit (collateral)      -> <transaction-hash>
4. Borrow                    -> <transaction-hash>
5. Repay                     -> <transaction-hash>
6. Release (collateral)      -> <transaction-hash>
```

Include script/code hashes and type script information for each of
`asset/`, `kyc-attestation/`, and `lending/`, plus any required environment
variables (see `.env.example`).

Transaction hashes should link to the CKB testnet explorer.
